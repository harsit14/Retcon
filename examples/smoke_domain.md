# Smoke Domain Note

Continuous pretraining experiments should keep data provenance, evaluation
manifests, and run configuration hashes close to the resulting metrics. This
small note exists only so the milestone 1 ingestion path has a deterministic
local document for smoke tests.
